<!doctype html>
<html lang="ru">
    {{>head}}
    <body>
        <div class="wrapper center">
            <div class="header">
                {{>header}}
            </div>
            <div class="content">
                <h1>Реєстрація нового користувача</h1>
                <p>
                    Якщо у вас вже є обліковий запис, увійдіть, будь ласка, на сторінці <a href="/auth/login">входу</a>.
                </p>
                <p>
                    Усі поля форми, наведеної нижче, є необхідними для заповнення:
                </p>
                {{#error}}<div class="warning center">{{error}}</div>{{/error}}
                <form id="register" class="default-form center" action="/auth/register" method="POST">
                    <label>
                        <span>Повне ім'я:</span>
                        <input type="text" placeholder="від 3 до 30 символів" minlength="3" maxlength="30" size="16" name="fullname" required>
                    </label>
                    <label>
                        <span>E-Mail:</span>
                        <input type="email" placeholder="Введіть email" size="16" name="email" required>
                    </label>
                    <label>
                        <span>Номер телефону:</span>
                        <input type="tel" placeholder="формат +380ххххххххх" pattern="\+380[0-9]{9}" size="16" name="phone" onclick="if(!this.value)this.value='+380'" required>
                    </label>
                    <label>
                        <span>Логін:</span>
                        <input type="text" class="input-huge-placeholder" placeholder="від 5 до 20 символів з A-Z, a-z, 0-9 та _" minlength="5" maxlength="20" pattern="[A-Za-z_0-9]{5,20}" size="17" name="login" required>
                    </label>
                    <label>
                        <span>Пароль:</span>
                        <input type="password" placeholder="від 8 до 30 символів" minlength="8" maxlength="30" size="16" name="pasw" required>
                    </label>
                    <label>
                        <span>Підтвердження паролю:</span>
                        <input type="password" placeholder="Введіть той самий пароль" minlength="8" maxlength="30" size="16" name="confirm_pasw" required>
                    </label>
                    <div>
                        <input type="reset" class="center" value="Скинути">
                        <input type="submit" class="center" value="Зареєструватися">
                    </div>
                </form>
            </div>
            <div class="footer">
                {{> footer}}
            </div>
        </div>
    </body>
</html>