<!doctype html>
<html lang="ru">
    {{>head}}
    <body>
        <div class="wrapper center">
            <div class="header">
                {{>header}}
            </div>
            <div class="content">
                <h1>{{info.title}}</h1>
                <p>{{info.message}}</p>
            </div>
            <div class="footer">
                {{> footer}}
            </div>
        </div>
    </body>
</html>