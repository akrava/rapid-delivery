<!doctype html>
<html lang="ru">
    {{>head}}
    <body>
        <header class="navbar">
            {{> header}}
        </header>
        <main class="container mt-4" role="main">
            <nav aria-label="breadcrumb">
               {{> breadcrumb}}
            </nav>
            <h1>Вхід</h1>
            <p>
                Якщо у вас ще немає облікового запису, зареєструйтесь, будь ласка, на сторінці <a href="/auth/register" class="link-style">реєстрації</a>.
            </p>
            {{#error}}
            <div class="alert alert-danger alert-dismissible fade show w-100 w-lg-40  mx-auto text-center" role="alert">
                <i class="fas fa-exclamation-triangle mr-1"></i>{{error}}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            {{/error}}
            {{#inform}}
            <div class="alert alert-success alert-dismissible fade show w-100 w-lg-40  mx-auto text-center" role="alert">
                <i class="far fa-check-circle mr-1"></i>{{inform}}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            {{/inform}}
            <form id="login" class="needs-validation mx-auto form-default mb-4 p-4"  action="/auth/login" method="POST" novalidate>
                <div class="form-row">
                    <div class="col-md-6 mb-1 form-group">
                        <label for="login_field">Логін:</label>
                        <input type="text" class="form-control" id="login_field" placeholder="Логін"  minlength="5" maxlength="20" pattern="[A-Za-z_0-9]{5,20}" name="login" aria-describedby="_helpBlock" required>
                        <div class="invalid-feedback">
                            Введіть правильний логін
                        </div>
                    </div>
                    <div class="col-md-6 mb-1 form-group">
                        <label for="password_field">Пароль:</label>
                        <input type="password" class="form-control" id="password_field" placeholder="Пароль" minlength="8" maxlength="30" name="password" aria-describedby="_helpBlock" required>
                        <div class="invalid-feedback">
                            Введіть правильний пароль
                        </div>
                    </div>
                    <small id="_helpBlock" class="form-text text-muted ml-1 mb-3">
                        Пароль повинен бути довжиною від 8 до 30 символів.<br/>
                        Логін, довжиною від 5 до 20 символів, складається з A-Z, a-z, 0-9 та _
                    </small>
                </div>
                <div class="d-flex">
                    <button class="btn btn-primary ml-auto mr-0" type="submit">Увійти</button>
                </div>
            </form>
        </main>
        <footer class="footer">
            {{> footer}}
        </footer>
        <script defer src="/javascripts/form-validation.js"></script>
        {{> scripts}}
    </body>
</html>