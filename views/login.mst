<!doctype html>
<html lang="ru">
    {{>head}}
    <body>
        <div class="wrapper center">
            <div class="header">
                {{>header}}
            </div>
            <div class="content">
                <h1>Вхід</h1>
                <p>
                    Якщо у вас ще немає облікового запису, зареєструйтесь, будь ласка, на сторінці <a href="/auth/register">реєстрації</a>.
                </p>
                {{#error}}<div class="warning center">{{error}}</div>{{/error}}
                {{#inform}}<div class="info center">{{inform}}</div>{{/inform}}
                <form id="login" class="default-form center" action="/auth/login" method="POST">
                    <label>
                        <span>Логін:</span>
                        <input type="text" minlength="5" maxlength="20" pattern="[A-Za-z_0-9]{5,20}" size="16" name="login" required>
                    </label>
                    <label>
                        <span>Пароль:</span>
                        <input type="password" minlength="8" maxlength="30" size="16" name="password" required>
                    </label>
                    <div>
                        <input type="submit" class="center" value="Увійти">
                    </div>
                </form>

            </div>
            <div class="footer">
                {{> footer}}
            </div>
        </div>
    </body>
</html>