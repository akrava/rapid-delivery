<!doctype html>
<html lang="ru">
    {{> head}}
    <body>
        <header class="navbar">
            {{> header}}
        </header>
        <main class="container mt-4" role="main">
            <nav aria-label="breadcrumb">
               {{> breadcrumb}}
            </nav>
            {{#registry}}
            <h1>Реєстр {{number}} - {{name}}</h1>
            <table class="mx-auto styled captioned py-2" id="registry">
                <caption>Реєстр #{{number}}</caption>
                <tbody>
                    <tr>
                        <td>Номер реєстру</td>
                        <td>{{number}}</td>
                    </tr>
                        <tr>
                        <td>Назва реєстру</td>
                        <td>{{name}}</td>
                    </tr>
                    {{#loginedUser.role}}
                    <tr>
                        <td>Автор</td>
                        <td>{{#user}}<a target="_blank" class="link-style" href="/users/{{login}}" class="ava"><img src="{{avaUrl}}" height="30" width="30" class="ava small">{{fullname}}</a>{{/user}}</td>
                    </tr>
                    {{/loginedUser.role}}
                    <tr>
                        <td>Опис</td>
                        <td>{{description}}</td>
                    </tr> 
                    <tr>
                        <td>Дата створення</td>
                        <td>{{created}}</td>
                    </tr>
                    <tr>
                        <td>Кількість накладних:</td>
                        <td>{{invoices.length}}</td>
                    </tr>
                </tbody>
            </table>
            <div class="content-bottom registry">
                <p>   
                    {{#invoices.0}}
                    Усього знайдено <b>{{invoices.length}}</b> накладних, які знаходяться в цьому реєстрі:
                    {{/invoices.0}}
                    {{^invoices.0}}
                    В цьому реєстрі ще немає жодної накладної.
                    {{/invoices.0}}
                </p>
                <ul class="list-inline">
                    {{#invoices}}
                        <li><a target="_blank" class="link-style" href="/invoices/{{number}}">{{number}}</a></li>
                    {{/invoices}}
                </ul>
            </div>
            <p class="text-center mt-3">Дії з реєстром:</p>
            <div class="actions default-form mb-3">    
                <form class="mx-auto form-inline pt-3 update-btn" action="/registries/edit" method="POST">
                    <input name="id" type="hidden" value="{{id}}">
                    <button class="btn btn-primary btn-sm mx-auto" type="submit">Редагувати реєстр</button>
                </form>
                <form class="mx-auto form-inline mt-2 pb-3 update-btn" action="/registries/remove" method="POST">
                    <input name="id" type="hidden" value="{{id}}">
                    <button class="btn btn-danger btn-sm mx-auto"  data-toggle="modal" data-target="#deleteModal" type="button">Видалити реєстр</button>
                    <!-- Modal -->
                    <div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="deleteModalLabel">Підтвердження видалення</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    Ви впевнені, що хочете видалити цей реєстр та ВСІ накладні, що входять до цього реєстру?
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрити</button>
                                    <button class="btn btn-danger" type="submit">Видалити</button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Modal END --> 
                </form>
            </div>
            {{/registry}}
        </main>
        <footer class="footer">
            {{> footer}}
        </footer>   
        {{> scripts}}
    </body>
</html>